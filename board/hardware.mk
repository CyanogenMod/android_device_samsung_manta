# TO-Do list
# figure out what this is and put it in the right location
BOARD_INV_LIBMLLITE_FROM_SOURCE := true
# grep to find if static hal is still a relevant thing
BOARD_HAL_STATIC_LIBRARIES := libhealthd.manta
# find the original removal commit and cherry pick it
BOARD_USE_CUSTOM_RECOVERY_FONT := \"roboto_23x41.h\"
